<Window x:Class="FRM.UI.FailuresSelection"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FRM.UI"
        mc:Ignorable="d"
                WindowStyle="SingleBorderWindow"
        WindowStartupLocation="CenterScreen"
        Title="Choose a failure type" Height="180" Width="300" ResizeMode="NoResize" FontSize="14" Background="#04c582">

    <Border Background="Black" CornerRadius="20" Margin="20,20,20,20">
        <StackPanel Margin="20,15,20,5">
            <ComboBox x:Name="comboBox" Width="200" SelectionChanged="comboBox_SelectionChanged" Background="CadetBlue" HorizontalContentAlignment="Center" KeyDown="comboBox_KeyDown">
                <ComboBox.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="CadetBlue"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Red"/>
                </ComboBox.Resources>
            </ComboBox>
            <Button x:Name="button" Content="Submit" Margin="0,15,0,0" Click="button_Click" Width="100" Height="30" Background="#545d6a" Foreground="White" FontSize="18" IsEnabled="False"/>


        </StackPanel>
        
        
    </Border>
    
    
</Window>
